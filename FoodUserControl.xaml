<UserControl x:Class="calendar.FoodUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:calendar"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="700">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <CheckBox x:Name="CheckBoxFood" BorderBrush="Teal" Foreground="Teal" Margin="5" HorizontalAlignment="Center"/>
        <Image x:Name="ImageFood" Grid.Column="1"/>
        <TextBlock x:Name="NameFood" Margin="10" Foreground="Teal" Grid.Column="2" Text="Завтрак/Обед/Перекус/Ужин" VerticalAlignment="Center" FontSize="20" />
    </Grid>
</UserControl>
